# No Claude/AI attribution in commit messages (house rule).
if grep -qiE "co-authored-by:.*(claude|anthropic)|generated with.*(claude|anthropic|ai)" "$1"; then
  echo "commit blocked: AI attribution in message is against repo policy" >&2
  exit 1
fi